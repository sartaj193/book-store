import express from "express";
//import { placeOrder } from "../controllers/orderController.js";
//import adminAuth from "../middlewares/auth.middleware";
//import { authUser } from "../middlewares/auth.js";
const router = express.Router();

//router.post("/place", placeOrder);
//router.post("/placeRazorpay", authUser,placeOrderRazorpay);
//router.post("/allOrde", adminAuth,allOrder);
//router.post("/showOrder",authUser,showOrder);
//router.post("/status",adminAuth, orderStatus);
//
export default router;

/*,placeOrderRazorpay,allOrder,showOrder,orderStatus*/
